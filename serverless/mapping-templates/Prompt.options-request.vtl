{
    "version" : "2017-02-28",
    "operation" : "Query",
    "index": "promptId-position-index",
    "query" : {
        "expression": "promptId = :id",
        "expressionValues" : {
            ":id" : {
                "S" : "${ctx.source.id}"
            }
        }
    },
    "limit": $util.defaultIfNull(${ctx.args.limit}, 20),
    "nextToken": $util.toJson($util.defaultIfNullOrBlank($ctx.args.nextToken, null))
}